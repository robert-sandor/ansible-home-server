- name: Create docker volume for Overseerr
  include_tasks: ../tasks/local_volume.yml
  vars:
    volume:
      name: overseerr-data
      type: local

- name: Copy service docker-compose file
  include_tasks: ../tasks/template_compose.yml
  vars:
    service: overseerr

- name: Start service
  include_tasks: ../tasks/compose_up.yml
  vars:
    service: overseerr
